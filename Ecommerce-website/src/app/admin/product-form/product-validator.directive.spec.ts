import { ProductValidatorDirective } from './product-validator.directive';

describe('ProductValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
